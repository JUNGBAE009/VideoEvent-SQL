# VideoEventSQL-AI-Video-Logger-for-SQL
VideoEventSQL is a lightweight Python library for logging video AI results into SQL. It converts YOLO, OpenCV, and NumPy outputs into structured events with labels, confidence scores, timestamps, bounding boxes, and metadata, making video analysis searchable, auditable, and automationready.
